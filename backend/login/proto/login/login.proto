syntax = "proto3";

package Login;

message LoginRequest {
  string username = 1;
  string password = 2;
}

message LoginResponse {
  bool success = 1;
}

service Login {
  rpc Login (LoginRequest) returns (LoginResponse);
}
